import { ResumenTextoPipe } from './resumen-texto.pipe';

describe('ResumenTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new ResumenTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
